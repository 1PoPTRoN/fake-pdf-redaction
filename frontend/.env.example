# Production API base. Set in Vercel project settings → Environment Variables.
# Find your URL in the Render dashboard after the first backend deploy
# (looks like https://pdfaudit-api.onrender.com).
VITE_API_BASE=https://pdfaudit-api.onrender.com/api/v1